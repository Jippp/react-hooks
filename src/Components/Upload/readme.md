## 文件上传

包含拖拽上传、单个文件上传、多个文件上传、大文件上传等需求

- [x] 拖拽上传

- [x] 单文件上传

- [x] 多文件上传
  - [x] 统一loading
  - [x] 统一上传

- [x] 大文件上传：分包
  - [x] 并发请求数限制

- [x] 取消上传

- [x] 取消的请求重新上传
  - [x] 新加一个接口，检查已上传部分

### 注意点
- loading
  - 是手动控制的
- 续传
  - 通过md5得出分片的hash，然后服务端检查上传了哪些，之后前端控制未上传的部分
- 大文件上传的时候会分包，所有包请求完成才会发送合并请求
  - Promise.all()监听时如果不做请求数量限制，会一次将所有的请求都通知到浏览器，等待浏览器的发送
  - 浏览器对请求数量作了限制，同一个域下最多同时发送6个请求
  - 如果这里不做限制的话，数量过多会导致卡死的情况
  - 应该做一层限制，比如等待4个或6个请求完成之后，再通知浏览器发送剩下的请求，直到所有请求发送完成
- 秒传
  - 没啥意义的高档词汇，即服务端已经存在相同资源，当然是秒传的🤣

- 服务端的合并
  - nodejs中通过stream来合并文件的，createReadStream pipe writeStream

### ✨feature
1. md5计算hash：一整个大文件计算的话会很浪费时间。有两种解决方案
   1. 因为是浏览器端上传，可以放到webWorker中计算
   2. 文件分片，每个分片单独计算hash
  > 由于在校验文件是否已上传需要hash，可以在拿到文件时候就开始计算hash，尽量不阻塞
2. 服务端文件的合并，现在会等到所有分片上传后在进行合并操作。
   1. 考虑是否可以一边上传一边进行合并操作
